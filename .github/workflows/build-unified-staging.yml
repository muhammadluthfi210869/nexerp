name: Test Unified Staging

on:
  push:
    branches:
      - integration/unified

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "Hello from test workflow"
