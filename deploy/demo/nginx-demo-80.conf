    # ── PROTOTYPE demo.nexerp.id (port 80: ACME + redirect) ──
    server {
        listen 80;
        server_name demo.nexerp.id;
        location ^~ /.well-known/acme-challenge/ {
            root /var/www/certbot;
            default_type "text/plain";
        }
        location / {
            return 301 https://$host$request_uri;
        }
    }
